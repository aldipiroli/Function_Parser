# Language-Processing

## Function Parser
A program that parses, defines and calls a function that can do sums and multiplications. 

### Example:
```
function f1(a,b,c){
	return a*b+c*a*b;
}

f1(1.0,2.0,3.0);
```
